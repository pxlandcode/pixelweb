import type { NavLink } from '$types';

const navLinks: NavLink[] = [
	{ label: 'For clients', href: '/#for-clients' },
	{ label: 'Join the team', href: '/about-us' },
	{ label: 'Contact', href: '#contact' }
];
export { navLinks };
